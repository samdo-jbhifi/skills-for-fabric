# Generated by build/gen_apm_manifests.py -- do not edit by hand.
#
# The installable APM package. Install one skill with:
#
#     apm install microsoft/skills-for-fabric --skill <skill-name>
#
# The package root is the repository, which places common/ inside it. That is
# what lets APM rewrite each skill's ../../common/ links to the shared files it
# materialises, so a single installed skill keeps every shared reference.

name: skills-for-fabric
version: 0.3.16
description: Microsoft Fabric skills for AI coding agents, with shared reference material.
author: Microsoft Fabric
license: MIT

# Explicit consent to deploy this package's local content.
includes: auto

# APM resolves MCP dependencies only from this root manifest, so every
# server the collection can need is declared here.
dependencies:
  mcp:
    - name: FabricIQ
      registry: false
      transport: http
      url: https://api.fabric.microsoft.com/v1/mcp/fabricaihub/integrations/m365
      headers:
        X-VARIANTS: "Fabric.Routing.PowerBIDataExploration"
      tools:
        - "*"
    - name: fabric-sqlendpoint
      registry: false
      transport: http
      url: https://api.fabric.microsoft.com/v1/mcp/dataPlane/sqlEndpoint
      tools:
        - "execute_query"
    - name: powerbi-modeling-mcp
      registry: false
      transport: stdio
      command: npx
      args: ["-y", "@microsoft/powerbi-modeling-mcp@latest", "--start"]
      tools:
        - "*"
